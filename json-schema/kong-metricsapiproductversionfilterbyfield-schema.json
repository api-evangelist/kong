{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "#/components/schemas/MetricsApiProductVersionFilterByField",
  "title": "Filter by api_product_version",
  "oneOf": [
    {
      "title": "Multiselect filters",
      "type": "object",
      "properties": {
        "operator": {
          "$ref": "#/components/schemas/RequestsFilterType"
        },
        "value": {
          "description": "The IDs to include in the results.\n",
          "type": "array",
          "items": {
            "$ref": "#/components/schemas/UnscopedUuid"
          }
        },
        "field": {
          "description": "The field to filter.",
          "type": "string",
          "enum": [
            "api_product_version"
          ]
        }
      },
      "required": [
        "operator",
        "value",
        "field"
      ]
    },
    {
      "title": "Empty filters",
      "type": "object",
      "properties": {
        "operator": {
          "$ref": "#/components/schemas/RequestsFilterTypeEmpty"
        },
        "field": {
          "description": "The field to filter.",
          "type": "string",
          "enum": [
            "api_product_version"
          ]
        }
      },
      "required": [
        "operator",
        "field"
      ]
    }
  ]
}
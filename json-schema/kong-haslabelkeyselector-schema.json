{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "#/components/schemas/HasLabelKeySelector",
  "title": "HasLabelKeySelector",
  "description": "Entity selector that includes entities by inclusion of a `label` key.",
  "type": "object",
  "properties": {
    "selector": {
      "type": "string",
      "enum": [
        "has_label_key"
      ]
    },
    "selector_parameters": {
      "type": "object",
      "properties": {
        "label_key": {
          "type": "string"
        }
      },
      "required": [
        "label_key"
      ],
      "title": "HasLabelKeySelectorParams"
    }
  },
  "required": [
    "selector",
    "selector_parameters"
  ]
}
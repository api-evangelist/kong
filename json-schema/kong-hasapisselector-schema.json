{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "#/components/schemas/HasApisSelector",
  "title": "HasApisSelector",
  "description": "Entity selector that includes services by attached APIs.",
  "type": "object",
  "properties": {
    "selector": {
      "type": "string",
      "enum": [
        "api_count"
      ]
    },
    "selector_parameters": {
      "$ref": "#/components/schemas/HasSelectorParams"
    }
  },
  "required": [
    "selector",
    "selector_parameters"
  ]
}
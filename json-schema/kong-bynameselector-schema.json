{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "#/components/schemas/ByNameSelector",
  "title": "ByNameSelector",
  "description": "Entity selector that includes entities by `name`.",
  "type": "object",
  "properties": {
    "selector": {
      "type": "string",
      "enum": [
        "name"
      ]
    },
    "selector_parameters": {
      "$ref": "#/components/schemas/StringFieldSelectorParams"
    }
  },
  "required": [
    "selector",
    "selector_parameters"
  ]
}